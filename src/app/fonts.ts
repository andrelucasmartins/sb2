import { Inter, Lexend } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], display: "swap", fallback: ["sans-serif"] });
export const lexend = Lexend({ weight: ["500"], subsets: ["latin"], display: "swap", fallback: ["sans-serif"] });